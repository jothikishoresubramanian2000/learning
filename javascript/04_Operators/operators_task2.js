const role = "admin";

console.log(role === "admin");
console.log(role == "admin");

const age = 16;

permission = age >= 18 ? "allowed":"denied"
console.log(permission)
const isActive = true;

isActive && console.log("welcome")